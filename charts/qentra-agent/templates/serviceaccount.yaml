apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "qentra-agent.fullname" . }}
  labels:
    {{- include "qentra-agent.labels" . | nindent 4 }}
{{- with .Values.imagePullSecrets }}
imagePullSecrets:
  {{- toYaml . | nindent 2 }}
{{- end }}
